<%= form_for(@group_comment, remote: true) do |f| %>
  <% if @group_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group_comment.errors.count, "error") %> prohibited this group_comment from being saved:</h2>

      <ul>
      <% @group_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>


  <div class="field">
    <%= f.hidden_field :group_post_id, value: @group_post.id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
