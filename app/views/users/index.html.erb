<%= stylesheet_link_tag "users", :media => "all" %>
<p id="notice"><%= notice %></p>

<body onload="getLocation()">
<div class="container">
  <div class="titleCon">Contacts</div>
<div class="section">
  <div class="subTitleCon">Close Friends</div>


  	
	<% @users.each do |user| %>
		<% unless user.id == current_user.id %>
		    <div class="fullSec">
		      <div class="usersSec col-md-3">
		        <%= image_tag(user.avatar_url(:thumb), class: "userImg") if user.avatar? %>
		        <span class="userNameNotTaken"><%= user.name %></span>
		        <span class="userNameNotTaken"><%= user.last_name %></span>
		        <span class="userNameNotTaken"><%= user.email %></span>
		      </div>

<br>
		     </div>
		<% end %>
    <% end %>
  

</div>

</div>



<hr>


<div class="container"><p id="geoloc"></p></div>


<hr>

</body>



<script>


	var x = document.getElementById("geoloc");



	function getLocation() {
	    if (navigator.geolocation) {
	      navigator.geolocation.getCurrentPosition(showPosition);
	      navigator.geolocation.getCurrentPosition(geoSuccess);
	    } else {
	      x.innerHTML = "Geolocation is not supported by this browser.";
	    }
  	}


	 
	function showPosition(position) {
		x.innerHTML = "Latitude: " + position.coords.latitude + 
		"<br>Longitude: " + position.coords.longitude;
	}



	  jQuery.ajax({
	    data: { lat: 5, lon: 6},
	    type: 'get',
	    url: "/users",   
	    dataType: 'JSON',

	  });





</script>





















