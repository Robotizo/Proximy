<%= stylesheet_link_tag "groups", :media => "all" %>
<%= form_for(@group.active_relationships_interests_to_groups.find_by(fdI_id: @interest.id), html: { method: :delete }, remote: true) do |f| %>
	<div class="buttonHov">
      <div class="followingState"><%= f.submit "Added", class: "btn  btn-primary addedInterest" %></div>
    </div>
<% end %>



